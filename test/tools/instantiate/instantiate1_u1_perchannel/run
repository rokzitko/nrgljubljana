#!/bin/sh
set -e

export PATH="$1:$PATH"

instantiate > instantiate.out

test -s data
test -s ham.0
test -s vec.0
test -s xi1.dat
test -s zeta1.dat
test -s theta1.dat

count=$(grep -c '^f ' data)
test "$count" = 2
