#!/bin/sh
set -eu
if "$1/broaden" -f 0 -o spec.bin 1 0.3 1e-4 >stdout.txt 2>stderr.txt; then
  echo "broaden unexpectedly accepted zero final width" >&2
  exit 1
fi
grep -q "Final Gaussian width must be greater than 0." stderr.txt
